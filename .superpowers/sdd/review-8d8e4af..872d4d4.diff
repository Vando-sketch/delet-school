# Review package: 8d8e4af0cb9add41bb6de4a444078eca7c643c50..HEAD

## Commits
872d4d4 refactor: extract shared ExecFileFn helper for subprocess injection

## Files changed
 src/lib/execFile.ts          | 9 +++++++++
 src/nextcloud/writeResult.ts | 7 +------
 2 files changed, 10 insertions(+), 6 deletions(-)

## Diff
diff --git a/src/lib/execFile.ts b/src/lib/execFile.ts
new file mode 100644
index 0000000..f669aa3
--- /dev/null
+++ b/src/lib/execFile.ts
@@ -0,0 +1,9 @@
+import { execFile as execFileCallback } from 'node:child_process';
+import { promisify } from 'node:util';
+
+export type ExecFileFn = (
+  file: string,
+  args: readonly string[],
+) => Promise<{ stdout: string; stderr: string }>;
+
+export const defaultExecFile: ExecFileFn = promisify(execFileCallback);
diff --git a/src/nextcloud/writeResult.ts b/src/nextcloud/writeResult.ts
index 0bb2555..ab3b2ba 100644
--- a/src/nextcloud/writeResult.ts
+++ b/src/nextcloud/writeResult.ts
@@ -1,26 +1,21 @@
 import { promises as fs } from 'node:fs';
-import { execFile as execFileCallback } from 'node:child_process';
 import * as path from 'node:path';
-import { promisify } from 'node:util';
 import pino from 'pino';
+import { defaultExecFile, type ExecFileFn } from '../lib/execFile.js';
 import { config } from '../config/index.js';
 import type { NextcloudWriter } from '../types.js';
 
 const logger = pino({ name: 'nextcloud-writer' });
 
 const RESULT_SUBFOLDER = 'teams-task-agent';
 
-type ExecFileFn = (file: string, args: readonly string[]) => Promise<{ stdout: string; stderr: string }>;
-
-const defaultExecFile: ExecFileFn = promisify(execFileCallback);
-
 export interface NextcloudWriterDeps {
   /**
    * Injection point for the `occ` invocation, used by tests to stub out the
    * CLI call without depending on a real Nextcloud install. Defaults to
    * Node's promisified `child_process.execFile`.
    */
   execFile?: ExecFileFn;
 }
 
 /**
